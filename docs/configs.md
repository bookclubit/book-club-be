1. Локальный конфиг `.vscode` призван переопределить глобальные настройки.
2. Мы стараемся делегировать основную валидацию `oxfmt` а для остальных форматов настраиваем `prettier`. Поэтому есть спец. настройки по форматам в .vscode и также добавляем `.prettierignore`, где исключаем все обратные форматы: те, которыми занимается `oxfmt`, а также автогенерацию.

P.S. warning `trailingComma` для .jsonc пока победить не удаётся. 3.
